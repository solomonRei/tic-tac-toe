package tictactoe.enums;

/** Enumerates the types of players in the game. */
public enum PlayerType {
  /** Represents a human user player. */
  USER,
  /** Represents a computer-controlled player. */
  COMPUTER
}
